/*
 * @Description: 如果有时间可以写：把服务器配置放到json中，用这个类来配置server
 *               但是我们这个项目太简单了，好像没有什么要配置的
 *               TODO:解析json文件，赋值field,写getter和setter,setter应该需要同时修改json
 */
package src.server;

import java.io.File;
import java.io.IOException;

public class ServerConfig {
  int port;
}
